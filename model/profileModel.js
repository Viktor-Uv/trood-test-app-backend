const {db} = require("../firestore");

const create = (body, cb) => {};

const read = (id, cb) => {};

const readAll = (cb) => {};

const update = (id, body, cb) => {};

const remove = (id, cb) => {};

module.exports = {
  create,
  read,
  readAll,
  update,
  remove
};
